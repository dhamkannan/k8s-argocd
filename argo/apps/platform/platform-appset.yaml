apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform
  namespace: argocd
spec:
  generators:
  # Environments
  - list:
      elements:
      - namespace: dev
      - namespace: prod
      - namespace: platform
  template:
    metadata:
      name: '{{namespace}}-platform'
    spec:
      project: default
      sources:
      - ref: values
        repoURL: https://github.com/dhamkannan/k8s-argocd.git
        targetRevision: v1
      - path: helm-charts/inhouse/platform-core
        repoURL: https://github.com/dhamkannan/k8s-argocd.git
        targetRevision: v1
        helm:
          valueFiles:
            - values.yaml
            - $values/argo/values/namespaces/{{namespace}}.yaml
      destination:
        namespace: platform
        server: https://kubernetes.default.svc
    # {{- if .helm.syncPolicy.enabled }}
    # syncPolicy:
    #   automated:
    #     prune: {{ .helm.syncPolicy.prune }}
    #     selfHeal: {{ .helm.syncPolicy.selfHeal }}
    # {{- end }}
 